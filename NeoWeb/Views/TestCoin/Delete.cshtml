@model NeoWeb.Models.TestCoin
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<SharedResource> SharedLocalizer

@{
    ViewData["ParentTitle"] = SharedLocalizer["Developer"].Value;
    ViewData["Title"] = "Delete TestCoin Apply";
}

<div class="py-5">
    <p>Are you sure you want to delete this?</p>
    <p class="mt-3">@Html.DisplayFor(model => model.Name)</p>
    <p class="mt-3">@Html.DisplayFor(model => model.Email)</p>
    <div class="mt-3">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn mt-3" />
            <div class="mt-3">
                <a asp-action="list">Back to List</a>
            </div>
        </form>
    </div>
</div>
